<div class="exercise-wrapper text-center">
  <h1 class="exercise-title">運動ログ一覧</h1>

  <% if current_user.email == "guest@example.com" && current_user.exercise_logs.exists? %>
    <p>✨体験版モードです</p>
    <p>これ以上投稿するには会員登録してください</p>

    <div class="guest-actions">
      <%= link_to "投稿一覧を見る", posts_path, class: "btn btn-secondary" %><br>
      <%= link_to "新規登録する", new_user_registration_path, class: "btn btn-primary" %>
    </div>
  <% else %>
    <%= link_to "今日の投稿を記録する", new_exercise_log_path, class: "btn btn-primary" %>
  <% end %>

  <p>累計ポイント：<%= current_user.growth_logs.sum(:growth_point) %> pt </p>

  <% if @exercise_logs.any? %>
    <ul>
      <% @exercise_logs.each do |log| %>
        <li>
          <%= I18n.t("enums.exercise_log.category.#{log.category}") %>
          （<%= log.minutes %>分）
        </li>
      <% end %>
    </ul>
  <% else %>
    <p>まだログがありません</p>
  <% end %>
</div>