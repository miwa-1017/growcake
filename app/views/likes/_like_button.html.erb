<% if user_signed_in? && current_user != post.user %>
        <div class="like-area">
          <% if current_user.liked?(post) %>
            <%= button_to "いいね解除💔 #{post.likes.count}",
                post_like_path(post),
                method: :delete,
                remote: true,
                class: "btn like-btn liked" %>
          <% else %>
            <%= button_to "いいね❤️ #{post.likes.count}",
                post_like_path(post),
                method: :post,
                remote: true,
                class: "btn like-btn" %>
          <% end %>
        </div>
      <% end %>