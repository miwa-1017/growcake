<!DOCTYPE html>
<html>
  <head>
    <title>Growcake</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <link href="https://fonts.googleapis.com/css2?family=Zen+Maru+Gothic:wght@300;500&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/7779a18b96.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
      <header class="site-header d-flex justify-content-between align-items-center px-4 py-3">

          <!-- ロゴ（左寄せ） -->
          <div class="header-logo">
            <%= link_to root_path do %>
              <%= image_tag "growcake_logo.png", class: "logo-img" %>
            <% end %>
          </div>

          <!-- ナビゲーション（右寄せ） -->
          <nav class="header-nav header-nav d-flex align-items-center">
              <% if user_signed_in? %>
                <% if current_user.email == "guest@example.com" %>
                  <span class="welcome-text me-4">ようこそ、ゲストさん</span>
                <% else %>
                  <span class="welcome-text me-4">ようこそ、<%= current_user.name %> さん</span>
                <% end %>
              <% end %>

            <%= link_to "About", about_path, class: 'nav-link me-3' %>

            <%= link_to "投稿一覧", posts_path, class: 'nav-link me-3' %>

            <% if user_signed_in? %>
              <% if current_user.email == "guest@example.com" %>
                <%= link_to "ゲストログイン中", "#", class: "disabled-link" %>
              <% else %>
                <%= link_to "マイページ", mypage_path, class: "nav-link me-3" %>
                <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "nav-link me-3" %>
              <% end %>
            <% else %>
              <%= link_to "ゲストログイン", users_guest_sign_in_path, method: :post, class: "nav-link me-3" %>
              <%= link_to "ログイン", new_user_session_path, class: "nav-link me-3" %>
              <%= link_to "新規登録", new_user_registration_path, class: "nav-link me-3" %>
            <% end %>

            <% if user_signed_in? %>
              <% if current_user.admin? %>
                <%= link_to "ユーザー管理", admin_users_path, class: "nav-link me-3" %>
              <% end %>
            <% end %>

          </nav>
       </header>
    <%= yield %>
  </body>
</html>
